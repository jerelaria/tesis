# ============================================================================
# 11. Few-shot iterative, 4 refs + refinement
# Best iterative config. Compare vs config 8 (indep 4ref refine).
# ============================================================================
experiment:
  name: fs_iter_4ref_refine
  description: "Few-shot iterative, 4 refs + recovery + improvement"

mode: few_shot

dataset:
  extensions: ["png"]

segmenter:
  model: medsam2
  device: cuda
  grid_side: 6
  score_threshold: 0.50
  iou_threshold: 0.50

extractor:
  method: moments
  features: ["V", "Cx", "Cy", "Dx", "Dy", "L"]

labeler:
  algorithm: "kmeans"
  features: ["V", "Cx", "Cy", "Dx", "Dy", "L"]
  kmeans:
    random_state: 42

cluster_filter:
  min_image_frequency: 0.3
  min_avg_labeling_confidence: 0.5
  min_avg_sam_confidence: 0.6
  deduplicate_per_image: true
  dedup_sam_score_weight: 0.2

refinement:
  enabled: true
  min_cluster_confidence: 0.5
  min_image_frequency: 0.3
  mask_selection:
    sam_score_weight: 0.5
    min_combined_score: 0.5
    num_reference_frames: 3
  improve_existing: true
  improve_min_combined_score: 1.0
  improve_sam_score_weight: 0.2

few_shot:
  propagation_mode: iterative